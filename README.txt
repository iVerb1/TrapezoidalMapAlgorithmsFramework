Using the menu bar you can open an input file and select an algorithm to run

You can also save the current configuration, algorithm - input file
The next time the application is started this configuration is loaded by default
when saving a configuration for the first time, a file config.conf is created in the src folder
